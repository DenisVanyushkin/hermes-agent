# LinkedIn: классификация исхода страницы по режиму — план реализации

> **Execution contract:** выполнять пункт за пунктом; на каждое изменение поведения —
> TDD с зафиксированным красным прогоном; перед каждым заявлением о готовности —
> проверка, а не намерение. Завершить, проверить и закоммитить один ограниченный
> срез прежде чем начинать следующий. Зелёный прогон тестов никогда не означает
> продуктового или продового одобрения.

**Дата:** 2026-09-02
**Авторы:** Claude и Codex. Процедурная основа раздела A, раздел B и пункт C1 —
авторства Codex; предмет, порядок, инварианты, C2 и раздел D — авторства Claude

**Три состояния, которые легко спутать, и потому названы раздельно:**

| Состояние | Значение на 2026-09-02 |
|---|---|
| Санкция на объём | **выдана частично**: владелец разрешил разделы A (офлайн-разбор) и B (bounded live probe). Разделы C и D **не разрешены** |
| Ревизия авторов | **завершена** 2026-09-02: Claude и Codex сошлись на предмете, порядке и приёмке; технических возражений не осталось |
| Исполнение | **разрешено до конца раздела B включительно.** Раздел C — решение владельца, не работа исполнителя. Раздел D требует отдельной авторизации на защищённый acquisition path |

**Что именно разрешено, дословно.** Владелец 2026-09-02: «свою отдельную санкцию
я даю сразу, включите в план, что уже разрешено». Прочитано как разрешение на
checkpoint 1 и checkpoint 2 (разделы A и B).

**Дополнительная авторизация, выданная 2026-09-02** (после первой редакции
плана): владелец разрешил **одноразовый probe-driver вне репозитория** для
раздела B. Разрешение выдано в ответ на прямо поставленный вопрос, покрывала ли
санкция на пробу написание драйвера, раз discovery объявлялся как «код не
пишем». Драйвер живёт вне репозитория, не меняет `job_intel/` и по окончании
раздела B не становится частью кодовой базы.

**Что НЕ разрешено и почему это не придирка:**

| Не выдано | Почему нельзя вывести из выданного |
|---|---|
| Measurement-policy решение (C1) | Оно требует данных пробы. Дать его заранее — значит выбрать семантику до наблюдения, то есть ровно тот дефект, который план чинит |
| Правка защищённого acquisition path (D) | Отдельная авторизация. Прежнее исключение на этот путь закончилось вместе с закрытием Gate A и не возобновлялось |

**Baseline живого чекаута:** `3efe7aec5b3c7d4fc216eadc42948775da31cdef`,
ветка `local/customizations`, `/home/hermes/.hermes/hermes-agent`
**Product SoT:** [`2026-08-10-job-intel-search-product-redesign-design.md`](../superpowers/specs/2026-08-10-job-intel-search-product-redesign-design.md),
версия `1.0.0`, статус `Approved`,
SHA-256 `430340de2613ee733926d73ce276c93676fe64b1841bb2f68f3f9303b61fc3a8`
**Репозиторий issues:** https://github.com/DenisVanyushkin/hermes-agent

**Goal:** сделать так, чтобы исход страницы LinkedIn определялся по наблюдённой
потере результатов, а не по режиму аутентификации — чтобы Gate A перестал
обрывать собственный сбор и рапортовать этот обрыв как ненаблюдаемость рынка.

---

## Предмет

> **Контекст, не пункт работ.** Фиксирует наблюдения, из которых вырос план. Цитаты и DoD не имеет намеренно.

В прогоне `gate-a-20260829T064036Z` (`0740165165a517fad40ea9ac443da094e178d06a`)
собрано 1532 уникальные вакансии, 1114 компаний, 1437 зачтённых записей. При
этом **все 24** географические ячейки имеют исход `degraded` с единственной
причиной `login_wall_or_auth_redirect`, а `product_observability_state` равен
`blocked` по всем 29 ячейкам.

В каталоге логов того же прогона **768 артефактов `page-0` и ни одного
`page-25`**. Все 96 запросов собрали нулевую страницу и остановились.

Причинная цепочка в `job_intel/browser_sourcing.py`:

1. `_looks_like_linkedin_results_page` (`:589`) распознаёт страницу результатов
   по трём CSS-классам — `job-card-container__link`,
   `job-card-list__title--link`, `artdeco-entity-lockup__subtitle`. Все три
   принадлежат **аутентифицированной** вёрстке.
2. Публичная вёрстка использует `base-search-card`. Измерено на четырёх
   сохранённых `cell-uk-page-0.html`: по 122–123 вхождения `/jobs/view/`, по
   360 `base-search-card`, **по нулю всех трёх маркеров**, при
   `linkedin.com/jobs/search` в URL.
3. Следовательно `_page_has_source_results` (`:596`) на логауте всегда `False`,
   и защита в `BrowserSessionHealth.update` (`:192`) —
   `if vacancies_found > 0 and _page_has_source_results(...): login_wall = False`
   — не срабатывает никогда.
4. `_looks_like_login_wall` (`:696`) — подстрочный поиск по
   `f"{url} {html}".lower()` по списку, включающему `sign in`, `log in`,
   `login`, `checkpoint`, `to view more jobs`. Логаутная страница содержит
   собственный CTA «Sign in to view more jobs»: измерено 18 вхождений `sign in`
   и 14 `login` на странице с 299 `job-search-card`.
5. Счётчик `login_walls` растёт, условие на `:2088` видит прирост относительно
   снимка на `:1894`, и следующий `break` завершает цикл страниц.

**Естественный эксперимент, уже лежащий на диске.** Каталог
`b811cda2d4db7ee9e92e69e3e2490c17888ad9c0` содержит два прогона одного дня, на
одном коде, по одному и тому же набору из 24 ячеек:

| Прогон | Режим | `page-0` | `page-25` |
|---|---|---|---|
| `gate-a-20260828t164843z` | с сессией | 288 (768 с диагностикой) | **288 (768)** |
| `gate-a-20260828t183056z` | логаут, контроль | 288 (768) | **0 (0)** |

Единственное различие — наличие сессии. Пагинация ломается не LinkedIn'ом, а
собственным детектором.

**Следствие для Gate A.** `resolve_acquisition_outcome`
(`job_intel/product_search/acquisition_probe.py:104`) требует для
`candidate_records_found` условие `completed >= minimum_independent_families`,
а `degraded` в `completed` не входит. Пока LinkedIn в логауте всегда
`degraded`, он не вносит в порог ничего, и ни одна ячейка не может пройти Gate A
ни при каком сборе.

**Что этим отменяется.** Первый acceptance criterion issue
[#15](https://github.com/DenisVanyushkin/hermes-agent/issues/15) — «хотя бы одно
семейство помимо `linkedin` имеет исход `completed` и число полученных строк
больше нуля» — уже выполнен DuckDuckGo в ячейках `singapore` и `south_korea`
(`productive` определён на `:1785` как `outcome == "completed" and
received_records > 0`; per-family `credited` в формулу не входит). Второй
criterion при вечно деградировавшем LinkedIn требует уже двух семейств помимо
него. Issue внутренне противоречив и подлежит переформулировке после этого
плана, а не до него.

## Порядок исполнения и почему он не переставляется

> **Контекст, не пункт работ.** Объясняет зависимости между разделами. Цитаты и DoD не имеет намеренно.

Разделы A → B → C → D исполняются строго в этом порядке.

- **Раздел A идёт первым, потому что не требует ни байта нового трафика.**
  Парные артефакты обоих режимов уже сохранены по одним и тем же 24 ячейкам.
  Всё, что можно узнать без сети, должно быть узнано без сети: живая проба
  расходует бюджет запросов к внешнему сервису, и тратить его на вопрос,
  ответ на который лежит на диске, — расточительство и лишний риск.
- **Раздел B идёт после A, потому что A определяет, что именно спрашивать.**
  Список сохраняемых признаков и определение «usable page» должны быть
  выведены из наблюдённого контраста, а не назначены заранее.
- **Раздел C обязан стоять между B и D.** Проба способна показать стену на
  offset N+1, но не способна решить, означает ли это нормальное завершение в
  объявленном публичном потолке источника или critical degradation
  относительно Gate-плана. Это measurement-policy решение владельца. Принять
  его за него — значит написать критерий, выбрав семантику самостоятельно,
  то есть повторить чинимый дефект на уровне собственной работы.
- **Раздел D последний, потому что до C неизвестен GREEN.** RED возможен уже
  сейчас на существующей публичной `page-0`; GREEN — контракт публичной
  страницы на offset 25 и дальше — без наблюдённых артефактов был бы выдумкой.

## Общие инварианты

> **Контекст, не пункт работ.** Применяются ко всем разделам одновременно и реализуют execution contract плана, а не отдельное требование SoT.

- Каждая правка поведения сопровождается тестом, который **падает до** правки и
  **проходит после**; «тест написан» без зафиксированного красного прогона не
  засчитывается.
- Работа ведётся в отдельном worktree, не в живом чекауте: резидентный агент
  периодически делает `reset --hard` живого дерева. Отдельный worktree
  **обязателен** для работы с планом и репозиторием; raw discovery evidence
  живёт **вне репозитория**, в новом неперезаписываемом root.
- Отсутствие штатной итоговой строки или receipt означает
  `inconclusive` / `terminated`, а **не** pass. Это относится и к тестам, и к
  пробам.
- Написание плана покрыто текущей санкцией. **Коммит плана** и любая правка
  защищённого acquisition path требуют своих обычных ворот, если владелец не
  дал их отдельно.
- Полный `pytest` по репозиторию на VPS не запускается — он даёт loadavg ~50 и
  systemd-вотчдог убивает живой гейтвей. Только адресные наборы.
- Ограничение по времени задаётся внешним `/usr/bin/timeout`: плагина
  `pytest-timeout` в каноническом venv нет.
- Прогон без итоговой строки `pytest` считается **убитым**, а не «чистым».
- Ни один пункт не размаскировывает legacy-таймеры job-intel и не включает
  доставку владельцу.
- Ни один пункт не расширяет рукописный реестр `docs/company-registry-seed.yaml`.
- Ни один пункт не меняет `config/product_search/search_contract.v1.yaml`:
  состав семейств у ячеек — предмет отдельной работы по issue #15 после этого
  плана.

## Вне рамок

> **Контекст, не пункт работ.** Перечисляет то, что план сознательно не делает.

- Портфель источников: второе и третье broad-market семейство, судьба
  DuckDuckGo, восстановление HeadHunter. Это issue #15 и он переформулируется
  **после** раздела C, потому что сейчас его критерии противоречивы.
- Восстановление залогиненной сессии LinkedIn. Владелец решением 2026-08-29
  перевёл сбор в логаут по результату контрольного опыта; план это решение не
  пересматривает и не отменяет.
- Добор текста вакансии (issues #4, #5). После раздела B корпус меняется, и
  задачу нужно переоценить заново.
- Сохранение post-dispatch failure evidence отдельно от capability evidence,
  и `headhunter` как безымянная `lambda`, рвущая публикацию `last_trace`
  (`acquisition_probe.py:2038`). Оба — отдельные issue, оба реальны, ни один не
  блокирует этот план.
- Двойной дрейф теневого контура: коммит-пин и общий venv. Теневой сбор мёртв
  с 2026-08-30, но он исполняет **legacy** пайплайн и ни одного перехода
  Gate A не меняет. Срок — до старта пилота, не сейчас.
- Пригодность baseline-артефакта `7f1dc5c6f1` под §14.1. Дефект подтверждён
  обоими авторами, выносится отдельным issue.

---

## A. Офлайн-checkpoint: разбор сохранённых парных артефактов

**Разрешение:** выдано 2026-09-02. Нового трафика не требует.
**Процедурная основа раздела — авторства Codex.**

**Основание.** SoT §8.1:

> States 3 and 4 mean that the lane was not sufficiently observable. A single
> unsuccessful query can never establish state 2.

Определение «страница наблюдалась» должно опираться на свойство страницы, а не
на режим доступа. Материал для вывода этого свойства уже собран, и раздел
исполняется **одним проходом по одному корпусу с одним выходом**.

**Корпус.** Один SHA `b811cda2d4db7ee9e92e69e3e2490c17888ad9c0`: authenticated
`gate-a-20260828t164843z` и logged-out control `gate-a-20260828t183056z`. Оба
покрывают одни и те же 24 ячейки, различаются только наличием сессии.

**Ключи сопоставления:** `cell_id` / `query_id` / `offset`.

**Колонки сравнительной таблицы:** `mode`, `cell_id`, `query_id`, `offset`,
`requested_url`, `final_url`, artifact path / hash / bytes, DOM job-ID count,
`parsed_before_filter`, `returned`, `excluded`, `unexplained`,
`returned_outside_dom`, наличие company / location / salary / snippet /
description, public- и auth-DOM маркеры, CTA/wall маркеры, наблюдённый page
verdict.

**Обращение с raw HTML.** Raw authenticated HTML в репозиторий не копируется.
Raw evidence живёт вне репозитория. Будущие фикстуры выделяются только после
sanitization и secret scan.

### Инвариант анализа

> **Usable-result evidence берётся из DOM job IDs и pre-filter accounting и не
> зависит от `len(page_vacancies)`.**

Основание: `page_vacancies` на `:1974` — результат
`_qualify_linkedin_vacancies`, то есть счёт **после** executive role filter, и
именно он уходит в `_observe_page` на `:2077` как `vacancies_found`. Полная
рыночная страница без единой подходящей роли даёт `vacancies_found == 0`,
защита `:192` не может сработать в принципе, и страница снова будет объявлена
стеной. Признак, зависящий от постфильтрового счёта, воспроизводит чинимый
дефект в новом месте.

### Вопросы, на которые раздел обязан ответить

1. Какими наблюдаемыми признаками публичная страница результатов отличается от
   аутентифицированной, и какими — от настоящей стены.
2. Какие признаки присутствуют в **обоих** режимах на странице с результатами и
   отсутствуют на странице без результатов. Кандидат по умолчанию — валидные DOM
   job ID; он проверяется, а не принимается.
3. Сколько wall-маркеров списка `_looks_like_login_wall` содержит
   **аутентифицированная** страница результатов. Если их там ноль, это
   объясняет, почему дефект не проявлялся до перехода в логаут, и это надо
   записать, а не подразумевать.

### DoD раздела A

- [ ] Зафиксированы inventory и SHA-256 основных HTML/PNG/JSON:
      authenticated `page-0=288`, `page-25=288`; logged-out `page-0=288`,
      `page-25=0`. Отдельно указано, что вместе с goto/reuse/content-read
      diagnostics это `768/768` и `768/0`. Inventory воспроизводим по hashes.
- [ ] Сравнительная таблица заполнена по всем парным наблюдениям, с колонками и
      ключами выше. Поля public/auth сравниваются только там, где есть парные
      наблюдения.
- [ ] Три вопроса выше отвечены, каждое число сопровождается командой, которой
      получено.
- [ ] Развилка по постфильтровой ловушке закрыта одним из двух способов:
      **либо** найден естественный образец страницы с непустым DOM и нулём
      прошедших фильтр вакансий, и на нём показано, что выбранный признак
      классифицирует её как usable, а `vacancies_found` — нет;
      **либо** такого образца в корпусе нет, это зафиксировано как gap, и
      механически доказано, что выбранный evidence path не читает постфильтровый
      count.
- [ ] Названо, из каких уже вычисляемых на месте вызова величин берётся признак:
      `page_result.dom_unique_job_ids`, `pre_filter_vacancies`, результат
      `classify_linkedin_dom_job_ids`. Новых вычислений не вводится.
- [ ] Первый evidence gap назван: **отсутствие любого logged-out observation
      после offset 0**.
- [ ] Развилка по негативному образцу закрыта одним из двух способов.
      Объявленный корпус состоит из auth-results и public-results и **не
      содержит** объявленного genuine-wall образца, поэтому вопрос 1 выше нельзя
      закрыть на нём одном.
      **Либо** такой образец найден в существующих артефактах, хэширован и
      обоснован как допустимый негативный контроль — кандидат назван:
      `0740165165a517fad40ea9ac443da094e178d06a/logs/*-linkedin-auth-validate.html`
      из логаутного прогона, 96 файлов, измерено на одном: 53027 байт, `0`
      вхождений `/jobs/view/`, `0` карточек любого вида, `11` вхождений
      `sign in`; допустимость требует обоснования, потому что это страница
      **другого типа** (auth-validate, не search) и **другого run SHA**, чем
      объявленный парный корпус;
      **либо** допустимого образца нет, и это фиксируется как **второй
      evidence gap** для раздела B, а не восполняется выдуманным негативным
      контролем.
- [ ] Сформированы inputs для live manifest раздела B: список сохраняемых
      признаков выведен из анализа, а не назначен; для каждого вопроса раздела B
      заранее записано, что считается ответом.
- [ ] Перечислены вопросы, на которые офлайн-часть ответила полностью и живая
      проба не нужна.
- [ ] Выход раздела **один**, и он имеет **канонический receipt**:
      `<discovery-root>/a-receipt.json`. Receipt содержит абсолютные пути,
      размеры и SHA-256 трёх артефактов (inventory JSON, comparative table,
      answers-and-gaps документ), собственную версию схемы и время сборки.
      Хэшируется **сам receipt**; именно его hash фиксируется в live manifest
      раздела B. Composite «inventory + table + answers» без канонического
      receipt однозначного hash не имеет.
- [ ] Назван **точный перечень evidence gaps** — во множественном числе.
      Единственность gap не постулируется: развилки выше допускают их несколько.
- [ ] Repo, timers, units, env, shared и authenticated profiles,
      `company-registry-seed.yaml` не изменены.

---

## B. Predeclared low-volume live probe

**Разрешение:** выдано 2026-09-02 ровно на этот bounded scope.
**Автор раздела:** Codex.

**Цель.** Получить наблюдённый, а не придуманный контракт публичных страниц
LinkedIn **до** изменения кода: какие offsets дают usable result surface, где
впервые наблюдается terminal/wall-состояние, какие поля несёт карточка и чем
usable public page механически отличается от настоящей стены. Проба **не**
доказывает глобальный потолок LinkedIn и **не** меняет Gate A outcome.

**До первого байта трафика** записать manifest: commit и runtime SHA, resident
egress identity, blank isolated logged-out profile, выбранные cells `uk` и
`singapore`, правило выбора запроса — «первый объявленный LinkedIn query в
contract order для каждой cell» — его `query_id` и hash, offsets
`[0, 25, 50, 75, 100]`, navigation budget «10 search pages + не более 2
auth-state validations», timeout и stop policy.

**Через какой шов.** Каждый offset запрашивается независимо через существующий
`BrowserSourceClient.fetch_page` (`browser_sourcing.py:1573`, параметр
`capture_label` на `:1579`) или эквивалентный существующий capture seam —
потому что production loop **и есть предмет дефекта**. Это обход локального
ошибочного `break`, но **не** обход login/checkpoint/captcha или иных
ограничений LinkedIn.

**Чем нельзя.** `browser_worker fetch` как есть использовать запрещено:
`browser_worker.py:403` печатает `json.dumps({"ok": True, "html": html, ...})`,
то есть **весь HTML уходит в stdout** и может унести raw evidence в terminal и
journal. `fetch_page` пишет HTML/PNG/JSON с `capture_label` в новый
неперезаписываемый discovery-root, raw HTML в режиме `0600`, а stdout содержит
только bounded summary и hashes.

**Темп.** Встроенные browser delays не уменьшаются. Между двумя query sequences
выдерживается не менее 30 секунд. Retries запрещены. Detail pages запрещены.

**Что сохраняется после каждого offset:** requested и final URL, HTML hash и
bytes, DOM job IDs и их количество, pre-filter parser accounting, поля карточек,
CTA/wall маркеры, screenshot/JSON refs если capture seam их уже производит, и
classification — **как наблюдение, не как будущая policy**.

**Stop policy.** Немедленно остановить текущую sequence после сохранения первого
non-usable offset. Остановить **весь** probe, без сохранения страницы, при:
captcha или verify-human, `429` или rate-limit, browser safety / anti-bot
сигнале, неожиданной auth/session мутации, egress/runtime/manifest drift,
artifact-write failure, истечении `/usr/bin/timeout`. Никаких retries, смены
egress, логина, cookie injection или продолжения после safety stop.

**Как формулируется результат по глубине.** Если offsets 0–100 usable — результат
это **только** нижняя граница `public_depth >= 100`. Если первый non-usable
offset `K` наблюдён внутри bounded set — записывается contiguous usable range
до `K-25` и первое boundary observation `K`, **без** обобщения до глобального
потолка.

**DoD раздела B:**

- [ ] В live manifest зафиксирован hash выхода раздела A. Раздел B **не**
      переделывает inventory раздела A.
- [ ] Live manifest существовал **до** трафика.
- [ ] Фактические navigation counts не превышают budget.
- [ ] Для каждого **успешно захваченного non-safety** offset есть raw artifact и
      machine-readable row. Для offset, оборванного по safety-stop, сохраняется
      **bounded receipt без raw страницы** — stop policy запрещает сохранять
      captcha/429/safety-страницу, и DoD не вправе требовать обратного.
- [ ] Первый stop назван.
- [ ] Вновь полученные live public observations сопоставлены с saved auth
      observations **только при совпадающих keys** (`cell_id` / `query_id` /
      `offset`). Пункт не засчитывается выходом раздела A.
- [ ] Report отдельно отвечает: «наблюдали first non-usable boundary `K ≤ 100`»
      **либо** «доказали только lower bound `public_depth >= 100`». Слово
      «ceiling» без квалификатора в отчёте не употребляется: boundary — это
      наблюдение на выборке из десяти запросов, а не свойство LinkedIn.
- [ ] Raw artifacts в режиме `0600` и прошли secret scan.
- [ ] Slack, delivery и **LLM/evaluation-provider** calls равны нулю. Внешние
      навигации к LinkedIn нулю не равны по определению пробы и ограничены
      только объявленным navigation budget.
- [ ] Repo, timers, units, env, shared и authenticated profiles,
      `company-registry-seed.yaml` не изменены.

---

## C. Checkpoint владельца

**Разрешение:** НЕ выдано и не может быть выдано заранее — пункт требует данных
раздела B.

### C1. Measurement-policy решение: как трактовать наблюдённую публичную границу

**Автор пункта:** Codex. **Формулируется после раздела B, не раньше.**

**Вопрос владельцу:**

> Если для заранее объявленной logged-out sequence offsets от 0 до N дают
> usable, полностью сведённые страницы, а первый offset N+25 даёт
> login/checkpoint или wall без usable DOM job IDs — что должен означать этот
> boundary для Gate A?

**Вариант A — declared public ceiling является нормальным terminal outcome.**

Ceiling должен быть versioned и объявлен **до** прогона, а не вычисляться задним
числом из любого раннего wall. Usable offsets `0…N` — обязательный план,
`N+25` — отдельный boundary sentinel. Совпадение с объявленным terminal reason
даёт family `completed`; при `received_records > 0` оно также `productive`; wall
telemetry сохраняется, но не увеличивает `degraded` / `blocked`. Wall **раньше**
объявленной границы, неполный accounting или иной stop остаётся critical
degradation.

- *Плюс:* публичный источник может честно кредитоваться в Gate A в пределах
  собственной возможности.
- *Риск:* Gate начинает считать наблюдаемостью максимум источника, даже если он
  слишком мелок для рынка. Ceiling требует version- и freshness-политики.

**Вариант B — фиксированная требуемая глубина Gate важнее source ceiling.**

Любой wall до завершения всех planned offsets остаётся critical degradation;
family не входит ни в `completed`, ни в `productive`; полученные строки остаются
диагностическими.

- *Плюс:* строгая одинаковая глубина и fail-closed market claim.
- *Минус:* logged-out LinkedIn может оказаться навсегда непригоден для Gate A
  даже при большом полезном корпусе. Придётся возвращать сессию либо заменять
  семейство.

**Вариант C — развести execution completion и coverage sufficiency в две оси.**

Collection может быть `completed_at_public_ceiling`, но отдельный
`coverage_limited` не позволяет автоматически кредитовать его в
`candidate_records_found`. Таблица переходов и schema требуют отдельного
owner-approved редизайна, а не локальной правки предиката.

- *Плюс:* обе истины сохранены без переименования.
- *Минус:* самый широкий scope и новый state contract.

**Условие «границу не объявляем».** Если две ячейки показывают разные или
нестабильные boundaries, либо случились captcha / rate limit, либо первого
boundary до offset 100 нет — stable ceiling **не объявляется**: сохраняется
текущий fail-closed режим, implementation DoD не утверждается, задача
возвращается владельцу с названным evidence gap.

**Рекомендация A/B/C до раздела B не фиксируется** — ни Claude, ни Codex.

### C2. Пересмотр DoD раздела D

После ответа C1 приёмка раздела D либо подтверждается в текущем виде, либо
переписывается. Ни один тест раздела D не пишется до этого пункта.

**DoD:**

- [ ] Записано, какой именно вариант C1 выбран владельцем и какими данными
      раздела B он обоснован.
- [ ] Приёмка D зафиксирована в окончательном виде **до** написания первого
      теста.
- [ ] Если C1 меняет таблицу переходов, названо, какие пункты D добавились или
      исчезли, и почему.
- [ ] Если данные раздела B оказались недостаточны для ответа, это записано как
      результат, а не восполнено предположением: план останавливается здесь.

---

## D. Реализация

**Разрешение:** НЕ выдано. Требует отдельной авторизации на защищённый
acquisition path — прежнее исключение закончилось вместе с закрытием Gate A.

### D1. RED на существующей публичной странице

RED возможен уже сейчас и **не требует** выдуманной фикстуры второй страницы:
существующая публичная `page-0` честно ловит и ложную классификацию, и
преждевременный отказ даже попытаться offset 25.

**DoD:**

- [ ] Тест на sanitized публичной фикстуре из наблюдённых артефактов: страница
      с валидными DOM job ID и штатным sign-in CTA классифицируется как usable.
      На baseline падает.
- [ ] Тест на плане из двух offset: при usable первой странице код обязан
      запросить вторую. На baseline вызывается только offset 0 — падает.
- [ ] Оба красных прогона зафиксированы с указанием ревизии и причины падения.
      «Тест написан» без этого не засчитывается.

### D2. GREEN: исход страницы определяется наблюдённой потерей

**DoD:**

- [ ] Существование страницы определяется по улике **до** продуктового фильтра
      (`final_url`, `dom_unique_job_ids`, сведённый parser accounting), а не по
      `vacancies_found`.
- [ ] Кумулятивные счётчики `login_walls` / `auth_redirects` перестают быть
      основанием для page verdict и для control-flow `break`. Как телеметрия они
      остаются — докстринг `apply_linkedin_verdict` (`:752`) запрещает выводить
      по ним заключение, а не собирать их:

      > Счётчики login_walls и auth_redirects остаются нетронутыми: они
      > сохранены ради совместимости дашбордов, но перестают быть основанием
      > для вывода.
- [ ] Признак берётся из уже вычисленных на месте вызова величин; список
      CSS-имён не пополняется — чинить список именами значит воспроизвести
      дефект в новом месте.
- [ ] `completed_page_offsets == planned_page_offsets` там, где обе страницы
      usable. Проверяется собственным инвариантом кода (`:1911`, `:1959`,
      `:2105`), а не наличием файлов с именем `page-25`.
- [ ] Учёт извлечения сохраняет `unexplained == 0` и ноль returned-outside-DOM.

### D3. Три контроля, а не один

Один положительный образец и один отрицательный предикат не разделяют: предикат,
считающий usable всё, проходит все тесты D2. Но и предикат «нет job ID — значит
стена» проходит и положительный, и wall-тест, а затем склеивает
`completed`-с-нулём-строк с critical degradation — при том что этот же план
отделяет `productive` от `completed`.

**DoD:**

- [ ] **Контроль 1, настоящая стена на search-поверхности:** редирект на
      `login`/`checkpoint` либо HTML без usable result surface по-прежнему даёт
      critical degradation и обрывает план.
- [ ] **Контроль 2, auth-редирект:** покрывается отдельно и **не** засчитывается
      за контроль 1. Кандидат — `linkedin-auth-validate.html`; измерено, что это
      **другая поверхность**: в логаутном образце `linkedin.com/jobs/search` не
      встречается вовсе. Он доказывает ветку login/auth redirect и не доказывает
      search-wall.
- [ ] **Контроль 3, честно пустая выдача:** search URL и search-поверхность,
      ноль DOM job ID, ноль строк, **без** auth wall — обязана быть `completed`,
      а не `degraded`. Естественные образцы в корпусе есть: две логаутные
      `page-0` ячейки `tajikistan`
      (`20260828T183220.990249Z-…` 95067 байт и
      `20260828T184400.379226Z-…` 95187 байт), обе с `jobs/search` ×5, нулём
      `/jobs/view/` и нулём карточек.
- [ ] Зафиксировано, что отличить пустую выдачу от стены по тексту «ничего не
      найдено» **невозможно**: измерено, что в обоих образцах ноль вхождений
      `no matching jobs`, `no results`, `did not match`, `try different`,
      `broaden your search`. Предикат обязан опираться на что-то другое.
- [ ] Каждый из трёх контролей падает, если сделать предикат вырожденным в
      соответствующую сторону.

### D4. Узкий повторный прогон

**DoD:**

- [ ] Прогон на 2–3 **заранее названных** ячейках, логаутом, в той же
      композиции Gate A.
- [ ] В `logs/` появляются артефакты `page-25` там, где обе страницы usable.
- [ ] Исход Gate A фиксируется как **наблюдение**, а не как критерий приёмки.
      Требование «ячейка обязана выйти в `candidate_records_found`» в приёмку
      не входит намеренно: такой критерий создаёт стимул ослаблять предикат до
      нужного исхода, то есть встраивает в приёмку тот самый дефект, который
      план чинит.

---

## Финальная проверка и передача

- [ ] Каждый пункт DoD сопровождается командой, дающей вердикт, и её выводом.
- [ ] Разделы, которые не выполнялись из-за отсутствия авторизации, названы
      явно как невыполненные, а не опущены.
- [ ] Статус на передаче назван по шкале: `implemented_not_verified`,
      `verified_in_isolation`, `composition_verified`, `runtime_verified`,
      `deployed_not_verified`, `complete`.
**Informational follow-up, не пункт DoD.** Issue #15 внутренне противоречив —
основание в разделе «Предмет». Его переформулировка или закрытие **не входит** в
эту задачу: план объявляет #15 вне рамок, а мутацию GitHub владелец не
разрешал.

## Что этот план сознательно не доказывает

- Что публичный LinkedIn имеет какую-то конкретную глубину. Раздел B даёт
  наблюдённый потолок или нижнюю границу на выборке из десяти запросов, и это
  не свойство LinkedIn вообще.
- Что после фикса какая-либо ячейка пройдёт Gate A. Это наблюдение раздела D4,
  а не обещание плана.
- Что портфель источников достаточен. Он не достаточен, это issue #15, и план
  его не касается.
- Что теневой контур работает. Он не работает с 2026-08-30, и план его не
  чинит.
