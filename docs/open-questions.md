# Open Questions

This file records unresolved profile-architecture questions that are safe to keep in the repo.

Current policy for PR-1:
- `docs/job-intel/` is a future migration target, not a runtime requirement.
- `docs/profiles/` is also future-only for now.
- runtime validation should fail closed when a required field or policy field is missing.

Add new questions here only when they are durable and worth revisiting later.
