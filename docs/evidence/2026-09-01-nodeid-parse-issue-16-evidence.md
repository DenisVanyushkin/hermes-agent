objective: Complete the post-review checkpoint for issue #16 after repairing the runner's collection-error and status policies, narrowing the documented parser boundary, deriving the fail-closed selector from the shared parser, consolidating the two runtime-bundle stubs, and removing the duplicate skipped-summary test.

authorized_scope: In the isolated worktree `/home/hermes/worktrees/nodeid-parse-issue-16-review-fixes`, apply the six review fixes, create one candidate commit, run the §5 checkpoint on that candidate, and add this evidence packet. No live runtime publication, service restart, plan edit, or host mutation was authorized.

explicit_deferrals: Full-tree pytest remains forbidden by §0.3 because it overloads this host. Publication into `/home/hermes/.hermes/scripts/` remains a separate owner-authorized operation. The parser's supported class was not broadened. A behavioral proof of the finalizer's source-order boundary was not added: the existing source-order test remains explicitly source evidence only because the current finalizer harness does not distinguish availability filtering from probe execution; the real landing refusal remains covered behaviorally.

base_revision: c3f1902304cb9b76b6d41878f98fea3112916d6d

tested_candidate_revision: 38eddc2dce1e330ae56cfc1939aa59f43b3c94fc

evidence_commit_revision: SELF

changed_paths: Candidate `38eddc2dce1e330ae56cfc1939aa59f43b3c94fc` changes `scripts/run_tests_parallel.py`, `scripts/pytest_status_lines.py`, `tests/test_run_tests_parallel.py`, `tests/scripts/test_pytest_status_lines.py`, `tests/scripts/test_nodeid_fail_closed.py`, and `tests/scripts/runtime_bundle_test_utils.py`. The candidate restores the runner's call-site status set, recovers collection-error paths without the node-id bracket heuristic, narrows the parser docstring, derives the fail-closed selector through `parse_status_line`, replaces the bundle mini-parser/glob with one explicit helper tuple, and removes the duplicate skipped-summary test. The evidence commit adds only this file.

red_evidence: The two new runner tests were first run before the production fix with `timeout 120 /home/hermes/.hermes/hermes-agent/venv/bin/python -m pytest tests/test_run_tests_parallel.py -q -k "unpaired_path_bracket or rerun_out_of_collected" --tb=short`; exit 1, `2 failed, 21 deselected in 4.22s`. The failures were the declared causes: `collection_error_paths`/`error_count` were empty for `ERROR tests/test_[broken.py - ImportError: boom`, and `RERUN tests/a.py::test_flaky - transient` was collected. Receipt: `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-review-fixes/red-runner.txt`. The earlier checkpoint also supplied the independent bundle RED: on candidate C, `tests/scripts/test_upstream_sync_apply.py::TestEndToEndWithTheHostFinalizer::test_the_merge_lands_on_the_live_branch` exited 1 with `ModuleNotFoundError` because `pytest_status_lines.py` was missing from the apply stub bundle; receipt: `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-checkpoint-c/candidate-apply-focused.txt`. That defect was outside slice 2's owning suites because apply was not in them; the checkpoint therefore did its intended job.

green_evidence: The focused runner rerun on the tested candidate used the same named selection and exited 0 with `2 passed, 21 deselected in 1.57s`; receipt: `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-review-fixes-checkpoint/runner-review-green.txt`. It asserts `collection_error_paths == ["tests/test_[broken.py"]`, `error_count == 1`, and no collected node for `RERUN`. `tests/scripts/test_pytest_status_lines.py` and `tests/scripts/test_nodeid_fail_closed.py` passed in the focused run (`23 passed`); the boundary test records that `FAILED tests/test - old.py::test_case - boom` is outside the supported class without changing parser behavior. The fail-closed test now obtains its malformed selector by calling `parse_status_line` on `FAILED tests/test_selector.py::test_case[alpha - broken - boom`; it then executes the real runner probe, report door, classifier, and finalizer landing refusal in the existing harness. The explicit helper tuple is used by both apply and finalize stubs; their checkpoint suites pass. The existing `_SUMMARY_LINE` change was not moved or removed. A rollback check on the candidate accepted `============================== 1 passed in 0.01s ==============================`; replacing it with the pre-change pattern rejected the same input with `ValueError: pytest log has no summary line: the run was killed, not clean`. Receipt: `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-review-fixes-checkpoint/summary-line-rollback.txt`.

composition_evidence: `composition_verified` for the checkout. The previously accepted runner/report/gate composition remains the evidence for the three-slice vertical chain; this candidate's 12-set checkpoint was run sequentially from `/home/hermes/worktrees/nodeid-parse-issue-16-review-fixes` at exactly `38eddc2dce1e330ae56cfc1939aa59f43b3c94fc`, using `/home/hermes/.hermes/hermes-agent/venv/bin/python`. The fail-closed selector chain is continuously sourced from `parse_status_line` through the real runner probe and the real gate/finalizer refusal test, rather than from a hand-written node-report. The invariant receipt shows only the shared `_STATUS_RE` and the intentionally runner-owned collection-error `partition`; no residual `_FAILED_LINE`, `FAILED_LINE`, `_STATUS_VALUES`, or second status-line parser remains in the three production consumers. Receipt: `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-review-fixes-checkpoint/parser-invariant.txt`.

runtime_evidence: `runtime not published`. No command copied files into `/home/hermes/.hermes/scripts/`, and no service or path unit was touched. The acceptance-runtime dry-run passed and verifies the production ten-file `RUNTIME_FILES` contract; that is publication composition evidence, not proof that the host runtime changed. `deployed_not_verified` is not applicable because nothing was deployed. `complete` is not asserted.

full_gate_evidence: All 12 sets below were run sequentially on the same tested candidate, each under an external `timeout 300`, with a pytest summary and explicit exit in its receipt. The full-tree pytest was not run, explicitly by §0.3.

| Set | Command target | Final pytest line | Exit | Receipt |
|---|---|---|---:|---|
| gate | `tests/scripts/test_upstream_sync_gate.py` | `117 passed in 35.38s` | 0 | `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-review-fixes-checkpoint/candidate-gate.txt` |
| runner | `tests/test_run_tests_parallel.py` | `22 passed, 1 skipped in 10.78s` | 0 | `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-review-fixes-checkpoint/candidate-runner.txt` |
| sync-local | `tests/scripts/test_sync_local_customizations.py` | `16 passed in 19.83s` | 0 | `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-review-fixes-checkpoint/candidate-sync-local.txt` |
| selection | `tests/scripts/test_run_fork_tests_selection.py` with exact venv `/home/hermes/.hermes/hermes-agent/venv/bin/python` | `40 passed in 17.38s` | 0 | `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-review-fixes-checkpoint/candidate-selection.txt` |
| finalize | `tests/scripts/test_upstream_sync_finalize.py` | `1 failed, 75 passed in 144.84s`; by name: `TestRepoLock::test_rebase_script_refuses_to_run_while_repo_lock_is_held` | 1 | `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-review-fixes-checkpoint/candidate-finalize.txt` |
| apply | `tests/scripts/test_upstream_sync_apply.py` | `31 passed in 15.99s` | 0 | `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-review-fixes-checkpoint/candidate-apply.txt` |
| triage | `tests/scripts/test_upstream_sync_triage.py` | `24 passed in 3.56s` | 0 | `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-review-fixes-checkpoint/candidate-triage.txt` |
| slack | `tests/scripts/test_upstream_sync_slack.py` | `28 passed in 2.70s` | 0 | `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-review-fixes-checkpoint/candidate-slack.txt` |
| status-lines | `tests/scripts/test_pytest_status_lines.py` | `21 passed in 2.08s` | 0 | `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-review-fixes-checkpoint/candidate-status-lines.txt` |
| measure | `tests/scripts/test_measure_order_leakage.py` | `8 passed in 1.80s` | 0 | `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-review-fixes-checkpoint/candidate-measure.txt` |
| acceptance/runtime | `tests/scripts/test_acceptance_publish_runtime.py` | `2 passed in 1.71s` | 0 | `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-review-fixes-checkpoint/candidate-acceptance-runtime.txt` |
| fail-closed | `tests/scripts/test_nodeid_fail_closed.py` | `2 passed in 2.22s` | 0 | `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-review-fixes-checkpoint/candidate-fail-closed.txt` |

The only candidate failure is the known baseline failure named above. The baseline was obtained by a real sequential run of the eight historical sets, not by reading an old report, on separate worktree `/home/hermes/worktrees/nodeid-parse-issue-16-baseline-8dc` at `8dc8a8c48d4ffbf6d0fbc56a434549ccf9210e8a`. Its receipts are `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-baseline-8dc/`; it produced the same named finalize failure and no apply failure (`31 passed`). The candidate therefore has no new failing test name.

known_limitations: The supported parser class remains the one where any ` - ` belonging to a node id is at positive bracket depth; the zero-depth dash boundary is explicitly tested and not broadened. The two spec §3 limitations remain intentionally named and retained: an unmatched `[` may absorb diagnostic text into the node id, and an identifier containing `value] - inner` is grammatically ambiguous. The source-order test proves the ordering of the two finalizer assignments in source only; it does not claim execution-order proof. The host runtime remains the pre-candidate copy until separately authorized publication.

working_tree_state: Immediately before creating this packet, the candidate worktree at `/home/hermes/worktrees/nodeid-parse-issue-16-review-fixes` was clean (`git status --porcelain` empty) at `38eddc2dce1e330ae56cfc1939aa59f43b3c94fc`. The live checkout `/home/hermes/.hermes/hermes-agent` remained on branch `local/customizations`, HEAD `c3f1902304cb9b76b6d41878f98fea3112916d6d`, clean except for the pre-existing untracked `docs/plans/2026-08-22-upstream-sync-gate-review-fixes.md`. This packet is intended to be the direct child of the tested candidate and to be the only path added by the evidence commit.

pending_authority_or_decisions: Owner/Claude acceptance of this packet remains pending. Separate owner authorization is required to publish the candidate runtime bundle into `/home/hermes/.hermes/scripts/`; until then the host status is `runtime not published`. The runner status-set restoration is also recorded as a plan defect: §2's earlier composition instruction to remove the consumer status set contradicted spec §3's requirement that consumer policy remain at the call site; this candidate follows the spec and records the correction rather than silently treating the plan as authoritative.

## Issue §7 cross-check

| Issue criterion | Closing slice/checkpoint | Concrete evidence |
|---|---|---|
| Live `alpha - old` / `alpha - new` run yields two nodeids in report and log parsing | Slice 1 report door and slice 2 log door | Accepted slice packets under `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-slice-1/` and `/home/hermes/.hermes/evidence/nodeid-parse-issue-16-slice-2/`; candidate runner/gate suites remain green except the named baseline failure. |
| Both doors return equal node lists | Slice 2 | Accepted slice 2 live-parity receipt and candidate gate receipt `candidate-gate.txt` (`117 passed`). |
| Parsing is shared: one rule, not two similar rules | Slices 1–3, reinforced by this review candidate | Candidate invariant receipt has only shared `_STATUS_RE` plus the intentional runner-owned collection-error partition; candidate runner, gate, and measure suites pass. |
| Existing suites remain green | Checkpoint §5 on this candidate | The 12 receipts above; the sole failure is the exact baseline `TestRepoLock::test_rebase_script_refuses_to_run_while_repo_lock_is_held`, reproduced in the eight-set baseline. |
| Known parse refusal remains blocking, not silent | Slice 2, rechecked on this candidate | `candidate-fail-closed.txt` reports `2 passed`, exit 0; the real finalizer harness separately executes the unreadable merged-isolated probe refusal and no-landing assertion. |

status: `composition_verified` for the checkout; `runtime not published` for the host; `deployed_not_verified` not applicable; `complete` not set.
