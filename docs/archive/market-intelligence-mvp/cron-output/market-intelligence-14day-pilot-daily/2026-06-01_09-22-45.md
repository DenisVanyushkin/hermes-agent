# Cron Job: market-intelligence-14day-pilot-daily

**Job ID:** a92e081e1836
**Run Time:** 2026-06-01 09:22:45
**Schedule:** 15 9 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

You are running day-by-day operations for a 14-day market intelligence pilot. Work in /workspace/live-hermes. Do not add new features or new datasets.

Objective for this run:
1) Run the existing live market collector against the deployed journal at /var/lib/job-intel/state/job_intel.sqlite3.
2) Run the existing pilot operation so a Daily Market State Brief is generated from journal contents and persisted back into the journal.
3) Record daily operational metrics to a markdown file for this date.
4) Maintain a small state file so the pilot can be counted across 14 daily runs.

Use terminal commands only. Keep the run quiet unless there is an error.

Required steps:
- Set WORKDIR=/workspace/live-hermes.
- Use Asia/Almaty for report filenames via `TZ=Asia/Almaty`.
- Journal path: /var/lib/job-intel/state/job_intel.sqlite3
- State file: /var/lib/job-intel/state/trading_autopilot_pilot_14day_state.json
- Daily report directory: /workspace/live-hermes/docs/reports/pilot-daily

Procedure:
1) Ensure the daily report directory exists.
2) Run:
   python -m trading_autopilot.market_ingestion market-collect --journal-path /var/lib/job-intel/state/job_intel.sqlite3 --json
3) Run:
   python -m trading_autopilot pilot-run --journal /var/lib/job-intel/state/job_intel.sqlite3
4) Using Python + sqlite3, query the journal for:
   - total event counts by event_type
   - count of market.snapshot events
   - count of market.state_brief events
   - count of market.critical_alert events
   - per-source counts and latest observed_at for binance.spot, binance.futures, coinbase.spot
   - journal file size in bytes
5) Write a markdown snapshot named `docs/reports/pilot-daily/YYYY-MM-DD-pilot-metrics.md` containing:
   - date
   - journal path
   - journal size
   - event counts
   - source state/freshness
   - the exact stdout from `pilot-run`
6) Update the state file as JSON with fields:
   - start_date (set on first run, Asia/Almaty date)
   - last_run_date
   - run_count (increment by 1 every successful run)
7) If any command fails, exit non-zero and include the failing command in stderr.

Do not create the final evaluation report here; that will be done by a separate one-shot job on 2026-06-14.

## Response

[SILENT]
