# Spend Gate — llm-obs-1.1.0 Evaluation Run

**Status:** ОЖИДАЕТ РЕШЕНИЯ ВЛАДЕЛЬЦА. Live-вызовы заблокированы (`JOB_INTEL_LLM_LIVE_APPROVED` не установлен).
**Date:** 2026-07-23

## Что запускаем

Новый промпт `llm-obs-1.1.0` (уже реализован офлайн, commit `c5b0da03c`) —
одно изменение: mandate-факт не выводится из company-level доказательства.
Прогоняем по тому же замороженному calibration-корпусу, что и 1.0.0
(controls 158 / golden 21 / decision 20 = **199 кейсов**), чтобы сравнение
было честным.

## Почему ДВА прогона

Находка вариативности (5B-4): построчная повторяемость 30%, агрегатная
точность стабильна. Эффект правки промпта сопоставим с межпрогонным
разбросом, поэтому один прогон 1.1.0 против одного прогона 1.0.0 ничего не
докажет. Нужно 2 прогона 1.1.0 → сравнение диапазонов, а не точек.

## Смета (прайс подтверждён: $0.25/$2 за MTok)

| | Вызовы | Оценка | Worst-case |
|---|---|---|---|
| Прогон A (199) | 199 | ~$0.65 | $1.5 |
| Прогон B (199) | 199 | ~$0.65 | $1.5 |
| **Итого** | **398** | **~$1.3** | **$3** |

Идемпотентность записи переиспользует ~51 дубль на прогон, так что реальных
вызовов меньше. Прайс дороже допущения ×2 → стоп.

## Что произойдёт после approve

1. Прогон A live (1.1.0) → recordings в `/var/lib/job-intel/benchmark-artifacts/`
   (вне репозитория — после инцидента 2026-07-20).
2. Live-to-replay проверка A (ожидаем 199/199).
3. Прогон B live (1.1.0) в отдельную папку.
4. Пересчёт 5B-5 сравнения: 1.1.0 (2 прогона, диапазоны) vs 1.0.0 (2 прогона)
   vs deterministic — по осям mandate/company accept rate, precision, decision
   divergence. Ключевой вопрос: поднялся ли mandate accept rate с 0.38.
5. Отчёт + вердикт: 1.1.0 канонизировать / ещё итерация / deterministic
   остаётся. Это evidence для Step 5C, не выбор победителя.

## Explicit non-goals

Bounded/full historical (5B-6/5B-7) НЕ запускаются — это отдельные ворота.
Промпт после заморозки 1.1.0 не трогается в рамках этого прогона.

## Required owner decision

```text
APPROVE_5B_PROMPT_ITER_WITH_CAP_$3
REVISE_MODEL_OR_BUDGET
DO_NOT_RUN_LIVE_CALLS
```
